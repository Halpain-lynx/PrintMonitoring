@model IEnumerable<YourNamespace.Models.PrintEvent>

@{
    ViewData["Title"] = "Home Page";
}

<h1>Print Events</h1>

<table class="table">
    <thead>
        <tr>
            <th>Event Time</th>
            <th>User Name</th>
            <th>Printer Name</th>
            <th>Page Count</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.EventTime</td>
                <td>@item.UserName</td>
                <td>@item.PrinterName</td>
                <td>@item.PageCount</td>
            </tr>
        }
    </tbody>
</table>